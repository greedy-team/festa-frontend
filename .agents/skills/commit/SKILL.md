---
name: commit
description: 현재 Git 변경사항을 기능별로 나눠 커밋해 달라는 요청에 사용한다. 커밋 메시지 제안만 필요하거나 push·PR 생성 요청에는 사용하지 않는다.
version: 1.0.0
---

Read `../../../.claude/commands/commit.md` completely, then execute that workflow.

Treat the user's full request as `$ARGUMENTS`. Translate Claude-specific tool names and command references to their Codex equivalents. Ignore Claude-only model or thinking settings; current system and `AGENTS.md` instructions take precedence.
